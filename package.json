{
  "name": "edwin-leathers",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @edwin/web",
    "build": "npm run build --workspace @edwin/web",
    "start": "npm run start --workspace @edwin/web"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
